{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "tray-capability",
  "description": "Tray icon and menu permissions (app-scoped, no windows field)",
  "permissions": [
    "core:menu:default",
    "core:tray:default",
    "core:image:default"
  ]
}
